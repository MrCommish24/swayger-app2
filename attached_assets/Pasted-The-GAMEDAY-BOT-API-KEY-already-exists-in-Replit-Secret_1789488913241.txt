The `GAMEDAY_BOT_API_KEY` already exists in Replit Secrets for this project.

Do not create a new key.
Do not ask me to paste or reveal the secret value.
Do not display the value.

Please use the existing project secret named:

GAMEDAY_BOT_API_KEY

for the live production smoke test.

If the agent requires permission to access/use the secret, request permission to use the EXISTING secret only.

Once access is granted, rerun the live verification against:

https://www.swayger.app

STEP 1 — Confirm live archive code

Call the live archive endpoint against a deliberately nonexistent room UUID:

PATCH /api/gameday/rooms/00000000-0000-4000-8000-000000000000/archive

Headers:
- x-api-key: existing GAMEDAY_BOT_API_KEY
- X-Discord-Guild-ID: Swayger Test Server guild ID

The goal is to confirm that the NEW bot-authenticated archive logic is live.

Do not rely on deployment metadata.

STEP 2 — Create one disposable Madden smoke-test room

POST /api/gameday/rooms

Use:
- existing GAMEDAY_BOT_API_KEY
- X-Discord-Guild-ID matching the Swayger Test Server
- matching discord_guild_id in body
- test discord_channel_id
- test discord_user_id
- sport="madden"
- template_type="weekly_pick_card"
- 3 matchups
- at least one line_text
- enabled bonus with 2 answer options
- valid ISO deadline

Verify:
- ok=true
- room_id
- room_code
- public_link
- source="discord"
- sport="madden"
- template_type="weekly_pick_card"
- is_private=true
- correct guild/channel/user metadata
- pregame card open
- public link resolves
- room excluded from public discovery
- 3 matchup props
- 2 answer options per matchup
- bonus prop with 2 answer options

STEP 3 — Archive cleanup

Immediately archive that exact room with:

PATCH /api/gameday/rooms/:roomId/archive

using:
- existing GAMEDAY_BOT_API_KEY
- matching X-Discord-Guild-ID

Verify:
- archive succeeds
- archived_at populated
- second archive is idempotent
- room no longer active
- no active smoke-test fixture remains

STEP 4 — Guild-scope checks

Verify:
- missing guild header rejected
- missing body discord_guild_id rejected
- mismatched body/header guild IDs rejected

Important:
- Do not reveal the secret.
- Do not create a new secret.
- Do not modify code.
- Do not register Discord commands.
- Do not run the Discord bot.
- Do not post to Discord.

Return:

A. Whether the existing Replit secret was successfully used
B. Live archive probe result
C. Madden room creation result
D. Metadata/prop verification
E. Privacy verification
F. Guild-isolation results
G. Archive result
H. Idempotency result
I. Cleanup confirmation
J. Whether Claude can now register the Madden command in the test guild

End with:

EXISTING BOT SECRET USED: YES / NO
LIVE ARCHIVE CODE CONFIRMED: YES / NO
MADDEN DISCORD PRODUCTION SMOKE STATUS: PASS / FAIL
SAFE CLEANUP: PASS / FAIL
READY FOR CLAUDE TEST-GUILD REGISTRATION: YES / NO